/** @type {import('next').NextConfig} */
const nextConfig = {

};

export default nextConfig;

/*
  webpack: (config) => {
    config.externals = [...config.externals, 'bcrypt'];
    return config;
  }*/